export default function SectionTitleBar({ title }) {
    return (
        <div className="my-3 mx-side-side">
            <p className="text-base">
                {title}
            </p>
        </div>
    )
}